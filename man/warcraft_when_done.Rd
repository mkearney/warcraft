% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mode.R
\name{warcraft_when_done}
\alias{warcraft_when_done}
\title{Announce when an expression finishes}
\usage{
warcraft_when_done(expr, min_time = 0)
}
\arguments{
\item{expr}{An expression to evaluate. Wrap several statements in
\code{\{ \}}.}

\item{min_time}{Only announce if \code{expr} took at least this many
seconds. Defaults to 0, meaning always announce.}
}
\value{
The value of \code{expr}.
}
\description{
Runs \code{expr} and plays a clip when it is done: "job's done" on
success, an annoyed orc on failure. Unlike warcraft mode this times the
expression exactly, so it also works in scripts and inside functions.
}
\examples{
\dontrun{
## tell me when the model is fit
fit <- warcraft_when_done(lm(mpg ~ ., mtcars))

## only speak up if the block took more than a minute
warcraft_when_done({
  big_download()
  slow_model()
}, min_time = 60)
}
}
